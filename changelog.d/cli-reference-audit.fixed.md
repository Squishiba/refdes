- CLI reference: the top-level usage line now matches `refdes --help` — all twenty subcommands in help order (`serve`, `keys`, `calc-rewrite`, and `history` were missing, `serve` was misordered) and the global `--no-write` was absent from the synopsis.
- CLI reference: `refdes build`'s option table was missing `-v, --verbose`.
- CLI reference: the `refdes check` section claimed "nothing of the project's own is written", but a normal run mints missing surrogate `key:` fields and expands resolvable link/`check` targets to composite form; only `refdes --no-write check` writes none of that. The claim now matches observed behavior and points at the global flag.
- CLI reference: `refdes id --dry-run` was documented as writing nothing, but it still mints missing `key:` fields and refreshes `.refdes/schema.json`; the table row now says so and shows `refdes --no-write id --dry-run` as the write-free form.
- CLI reference: `refdes ls`'s free-text argument is `query ...` in help — zero or more words, joined into one query — not a single `QUERY`; the option table now matches.
- CLI reference: expanding a quoted bare-number `id:` hint leaves the quoted line in place next to the inserted full id, so the quoted value wins on the next load, `check` then reports `id: 007 has no prefix yet`, and a second `refdes id` rejects the item as a burned-number collision. Documented as a known, verified source bug so the documented flow is not trusted.
