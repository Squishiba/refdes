- Add `refdes keys adopt [--dry-run]` to transactionally mint surrogate keys, expand structured links, and re-key every baseline and nonempty seal file, with explicit uncomparable-entry reporting and byte-for-byte rollback on failure. A committed, self-describing `.refdes/keys-adopted.yaml` marker makes subsequent baselines and seals use immutable surrogate identity.
