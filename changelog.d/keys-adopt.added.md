- Add `refdes keys adopt [--dry-run]` to transactionally mint surrogate keys, expand structured links, and re-key every baseline and seal file, with explicit uncomparable-entry reporting and byte-for-byte rollback on failure. Adopted projects write new baselines and seals keyed by immutable surrogate identity.
