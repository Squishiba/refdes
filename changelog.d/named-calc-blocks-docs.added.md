- Document naming calc blocks (docs/design/named-calc-blocks.md Phase 5): the
  `id="..."` fence attribute in the math reference (opt-in, lowercase 1-40
  character name, a label on the rendering - not a scope, a key, or a value
  qualifier), the now-validated calc fence info string in the markdown
  reference, and the fence errors plus the `#calc:` fragment warning in
  troubleshooting.