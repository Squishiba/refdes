- docs/links.md's link-verb table now lists `part_of` (inverse `contains`),
  the `hardware@3` verb for declaring that a requirement, bound, decision,
  test, or component belongs to a `group`; the table had omitted it while
  the standard declared it.