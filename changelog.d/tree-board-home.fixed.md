The project tree files an item under its own board, not under its first
group: a boarded item in a project-wide group now expands inside a group
node within its board's branch (referenced under the group's own
expansion), and every boarded item is guaranteed to appear in its board's
branch, expanded or as a reference. Groups with members on several boards
show a node under each board holding only that board's members.
