2026-09-14
Started Threads Phase 2a. Fast-forwarded the worker branch from main successfully. Inspected the design, key/link pipeline, adoption flow, sealing, and existing tests before implementing the freeze pass.

Added bare-key target resolution, excluded bare follows targets from ordinary expansion, and added a one-time follows freeze plan using frozen edges only. It handles fork and sealed-entry warnings, deterministic date/file/position ordering, duplicate resulting targets, and --no-write gating.

`refdes keys adopt` composes the freeze plan into its transaction and reports frozen follows separately. Added regression coverage for tip selection, id-less tips, malformed/unknown key diagnostics, forks, D3 ordering, self-exclusion, rename-safe refresh, no-write/idempotence, sealed entries, ordinary links, duplicate merge targets, and adoption. Focused tests pass (48). The Python environment does not provide ruff, so its scoped style check could not run. Docs and changelog updated.

Rebased on main after Phase 2b landed. Reused `chains.build_graph(frozen_only=True)` and `chains.tips()` for the freeze pass; removed the temporary private walk. Added the minimal prospective-successor API needed to sequence same-load freezes. Focused follows/adoption tests pass (26) and chain tests pass (17). Pending final full-suite run.
