Task: implement `refdes keys adopt` from docs/design/keys.md §7.

Progress:
- Fast-forwarded from main before changes; worktree was clean.
- Factored write/restore primitives from revise.apply without changing its transaction ordering.
- Added write-free key and link source planners and source-preserving composition.
- Added the explicit adoption transaction, CLI and --dry-run output.
- Added `.refdes/keys-adopted` as the unambiguous adoption marker; keyed new baseline/seal shapes depend on it.
- Added tmp_path regression coverage for YAML/Markdown shapes, CRLF, baseline/seal carry-forward, named uncomparables, dry-run, idempotence, keyed future history, rename-safe seals, and sabotaged byte-for-byte rollback.
- Targeted adoption tests pass (5 passed); related existing key/lifecycle/seal tests pass (89 passed); the pre-rebase full suite passes (743 passed).

Difficulties:
- Key insertion changes line counts, so the transaction renders link edits first against original source coordinates while assigning keys first in memory. This preserves the required semantic order and source formatting.
- Imported artifacts still do not carry surrogate keys, so README's cross-project ID-collision warning remains a correctness requirement and was not relaxed.

Status: implementation, documentation, changelog, smoke verification, and pre-rebase full-suite verification complete.
